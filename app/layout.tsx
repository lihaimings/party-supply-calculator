import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/layout/Header'
import Footer from '@/components/layout/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Party Supply Calculator - Plan Your Perfect Party',
  description: 'Calculate food, drinks, and supplies needed for any party size. Never run out of refreshments or over-order supplies again.',
  keywords: 'party calculator, event planning, food calculator, drink calculator, party supplies',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  )
}
